<%= form_for@corgi, :html => {:multipart => true} do |f| %>
  <% if @corgi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@corgi.errors.count, "error") %> prohibited this corgi from being saved:</h2>

      <ul>
      <% @corgi.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
